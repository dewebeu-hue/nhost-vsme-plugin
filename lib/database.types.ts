// Placeholder for future generated Hasura GraphQL types.
//
// When the Nhost/Hasura backend is connected, replace this file with generated
// operation and schema types from the GraphQL schema. The UI remains mock-data
// driven until that integration step.

export type DatabaseTypesStatus = "pending_graphql_codegen";
