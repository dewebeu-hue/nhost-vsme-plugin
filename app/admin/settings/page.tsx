import { AdminPlaceholder } from "@/components/admin/admin-placeholder";

export default function AdminSettingsPage() {
  return (
    <AdminPlaceholder
      title="Settings"
      subtitle="Prepare admin workspace preferences, saved views, and team controls."
    />
  );
}
